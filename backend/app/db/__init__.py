"""Database layer: engine/session factory, declarative base, and ORM models.

The rest of the app depends on :mod:`app.domain.store` (the interface), never on
these modules directly — except the store implementation and Alembic.
"""
